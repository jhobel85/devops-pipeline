name: Build and Deploy to Local K3s

on:
  pull_request:
    branches:
      - master

jobs:                # ← column 1
  build-deploy:      # ← column 3 (two spaces in)
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker buildx build \
          -t devops-pipeline:latest \
          --file docker/Dockerfile \
          .

      - name: Save Docker image
        run: docker save devops-pipeline:latest -o devops-pipeline.tar

      - name: Import into containerd
        run: sudo ctr images import devops-pipeline.tar

      - name: Deploy to K3s
        run: |
          kubectl apply -f k8s/deployment.yaml

      - name: Verify deployment
        run: |
          echo "Checking K3s cluster status..."
          kubectl get nodes
